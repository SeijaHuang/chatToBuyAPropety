"""Internal service-layer result objects, one sub-package per endpoint.

These carry the outcome of a service-layer operation back to the router,
which shapes them into the corresponding HTTP response.
"""
